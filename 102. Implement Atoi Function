#include <bits/stdc++.h> 
int atoi(string str) {
    // Write your code here.
    int ans=0;
    for(auto it:str)
    {
        if(it>='0'&&it<='9')
            ans = (ans*10)+(it-'0');
    }
    if(str[0]=='-')
        ans *=-1;
    return ans;
}

// TC:-O(N)
// SC:-O(1)
